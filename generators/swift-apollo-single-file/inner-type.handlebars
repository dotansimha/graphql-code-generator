{{#each innerTypes}}
public struct {{name}}: GraphQLMappable {
  public let __typename = "{{ schemaTypeName }}"
  {{#each fields}}
  public let {{ name }}: {{#if isArray}}[{{/if}}{{ type }}{{#if isArray}}]{{/if}}{{#unless isRequired}}?{{/unless}}
  {{/each}}

  public init(reader: GraphQLResultReader) throws {
    {{#each fields}}
    {{ name }} = try reader.{{#if isRequired }}{{#if isArray}}list{{else}}value{{/if}}{{ else }}{{#if isArray}}optionalLiss{{else}}optionalValue{{/if}}{{/if}}(for: Field(responseName: "{{ name }}"))
    {{/each}}
  }

  {{> inner-type innerTypes=innerTypes ~}}
}

{{/each}}