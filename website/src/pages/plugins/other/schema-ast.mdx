import { PackageApiDocs, PackageHeader } from '@/components'
import { getNpmInfo } from '@/lib/stats'

export const getStaticProps = getNpmInfo('schema-ast')

<PackageHeader/>

## Examples

```yaml {9}
# …
schema:
  - 'http://localhost:3000/graphql'
  - './src/**/*.graphql'
  - 'scalar MyCustomScalar'
generates:
  path/to/file.graphql:
    plugins:
      - schema-ast
```

<PackageApiDocs/>
