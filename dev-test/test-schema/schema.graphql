type User {
  id: Int!
  name: String!
  email: String!
}

type Query {
  allUsers: [User]!
  userById(id: Int!): User

  # Generates a new answer for the
  # guessing game
  answer: [Int!]!
  testArr1: [String]
  testArr2: [String]!
  testArr3: [String!]!
}

