type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  createdAt: String!
  updatedAt: String!
}

enum RoleStatus {
  ADMIN
  USER
}

type mutation {
  createUser(name: String!, email: String!, password: String!): User!
}
