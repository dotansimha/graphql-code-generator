interface Plant {
  id: ID!
  species: String!
  age: Int!
}

type Flower implements Plant {
  id: ID!
  species: String!
  age: Int!
  color: String!
}

type Tree implements Plant {
  id: ID!
  species: String!
  age: Int!
  height: Float!
}
