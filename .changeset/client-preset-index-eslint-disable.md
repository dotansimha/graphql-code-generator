---
'@graphql-codegen/client-preset': patch
---

Add `/* eslint-disable */` to the generated `index.ts` barrel file, matching the other files generated by `client-preset` (`graphql.ts`, `gql.ts`, `fragment-masking.ts`), which already had it.
