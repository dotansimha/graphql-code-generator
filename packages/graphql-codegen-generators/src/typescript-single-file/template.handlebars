/* tslint:disable */
{{#each scalars}}
export type {{ name }} = any;
{{/each}}
{{#each types}}

export interface {{ name }} {
{{#each fields}}
  {{ name }}: {{ type }}{{#if isArray}}[]{{/if}}{{#unless isRequired}} | null{{/unless}};
{{/each}}
}
{{/each}}
