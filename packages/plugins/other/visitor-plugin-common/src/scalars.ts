import { NormalizedScalarsMap } from './types.js';

export const DEFAULT_SCALARS: NormalizedScalarsMap = {
  ID: 'string',
  String: 'string',
  Boolean: 'boolean',
  Int: 'number',
  Float: 'number',
};
