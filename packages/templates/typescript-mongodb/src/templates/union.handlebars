{{#ifDirective this "union" }}
{{ toComment description }}
export type {{ name }}DbObject = {{#each possibleTypes}}{{this}}DbObject{{#unless @last}} | {{/unless}}{{/each}};

{{/ifDirective}}
{{ toComment description }}
export type {{ name }} = {{#each possibleTypes}}{{this}}{{#unless @last}} | {{/unless}}{{/each}};