{{{ toComment description }}}
{{#if @root.config.enumsAsTypes }}
export type {{@root.config.interfacePrefix}}{{ toPascalCase name }} = {{#each values }}{{{ getEnumValue ../name value }}}{{#unless @last}} | {{/unless}}{{/each}};
{{else}}
export {{#if @root.config.constEnums }}const {{/if}}enum {{@root.config.interfacePrefix}}{{ toPascalCase name }} {
{{#each values }}
  {{ toPascalCase value}} = {{{ getEnumValue ../name value }}},
{{/each}}
}
{{/if}}
