{{ toComment description }}
{{#if @root.config.enumsAsTypes }}
export type {{@root.config.interfacePrefix}}{{ toPascalCase name }} = {{#each values }}"{{ value }}"{{#unless @last}} | {{/unless}}{{/each}};
{{else}}
export enum {{@root.config.interfacePrefix}}{{ toPascalCase name }} {
{{#each values }}
  {{value}} = "{{ value }}",
{{/each}}
}
{{/if}}
