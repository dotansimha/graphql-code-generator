{{ toComment description }}
{{#if @root.config.enumsAsTypes }}
export type {{ toPascalCase name }} = {{#each values }}"{{ value }}"{{#unless @last}} | {{/unless}}{{/each}};
{{else}}
export enum {{ toPascalCase name }} {
{{#each values }}
  {{value}} = "{{ value }}",
{{/each}}
}
{{/if}}
