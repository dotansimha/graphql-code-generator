{{ toComment description }}
export interface {{@root.config.interfacePrefix}}{{ toPascalCase name }}{{#if hasInterfaces}} extends {{#each interfaces}}{{@root.config.interfacePrefix}}{{ toPascalCase this }}{{#unless @last}},{{/unless}}{{/each}}{{/if}} {
{{#each fields}}
  {{#if @root.config.immutableTypes }}readonly {{/if}}{{ name }}{{ getOptionals this }}: {{ convertedType this }}; {{ toComment description }}
{{/each}}
}
