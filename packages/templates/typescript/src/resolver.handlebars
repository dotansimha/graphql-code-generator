{{ toComment description }}
{{#if @root.config.noNamespaces}}
export interface {{ toPascalCase name }}Resolvers {
  {{#each fields}}
  {{ name }}?: {{ toPascalCase ../name }}{{ getFieldResolverName name }}; {{ toComment description }}
  {{/each}}
}

{{#each fields}}
export type {{ toPascalCase ../name }}{{ getFieldResolverName name }} = {{ getFieldResolver ../name this }};

{{~# if hasArguments }}

export interface {{ toPascalCase ../name }}{{ toPascalCase name }}Args {
{{#each arguments}}
  {{ name }}{{ getOptionals this }}: {{ convertedType this }}; {{ toComment description }}
{{/each}}
}

{{/if}}
{{/each}}
{{else}}
{{ toComment description }}
export namespace {{ toPascalCase name }}Resolvers {
  export interface Resolvers {
    {{#each fields}}
    {{ name }}?: {{ getFieldResolverName name }}; {{ toComment description }}
    {{/each}}
  }

  {{#each fields}}
  export type {{ getFieldResolverName name }} = {{ getFieldResolver ../name this }};

  {{~# if hasArguments }}

  export interface {{ toPascalCase name }}Args {
  {{#each arguments}}
    {{ name }}{{ getOptionals this }}: {{ convertedType this }}; {{ toComment description }}
  {{/each}}
  }

  {{/if}}
  {{/each}}
  
}
{{/if}}