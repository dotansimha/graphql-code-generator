{{#each fields}}
{{#if @root.config.immutableTypes }}readonly {{/if}}{{ name }}{{ getOptionals this }}: {{#if @root.config.noNamespaces}}{{#unless (isPrimitiveType this)}}{{#unless this.isScalar}}{{toPascalCase ../prefix}}{{/unless}}{{/unless}}{{/if}}{{ convertedType this }}; {{ toComment description }}
{{/each}}
